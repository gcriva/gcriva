general:
  artifacts:
    - "coverage"

machine:
  environment:
    PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"
  services:
   - mongodb
  node:
    version: 8.2.1

dependencies:
  override:
    - npm install

test:
  override:
    - npm run validate
